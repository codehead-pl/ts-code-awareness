{
  "name": "@fixture/worker",
  "version": "0.0.1",
  "description": "Second package: cross-package Repository implementer + BaseService subclass",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "echo \"no tests\""
  },
  "dependencies": {
    "@fixture/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
