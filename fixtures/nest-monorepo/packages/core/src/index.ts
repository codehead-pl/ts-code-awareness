/** Barrel export for the shared core library. */
export { Repository, ListableRepository } from './repository';
export { BaseService } from './base-service';
export { Id, IsoDateString, Role, Entity, User } from './types';
